import { microserviceShared } from './microservice-shared';

describe('microserviceShared', () => {
    it('should work', () => {
        expect(microserviceShared()).toEqual('microservice-shared');
    });
});
